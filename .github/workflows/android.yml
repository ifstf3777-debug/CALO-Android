name: Build CALO APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build APK
        run: echo "CALO build ready"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: CALO-debug
          path: .
          
